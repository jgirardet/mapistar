import pytest

from mapistar.main import IsAuthenticated


# def test_is_authenticated(mocker):
#     req = mocker.MagicMock()
#     req.headers = {"Authorization": "Bearer 53151"}
#     t = IsAuthenticated().process_request(req, req)
#     assert True
